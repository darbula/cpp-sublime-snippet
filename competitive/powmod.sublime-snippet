<snippet>
  <description>Pow Mod</description>
  <content><![CDATA[${1:ll} powmod(int a, int b, int mod) {
  ${1:ll} x=1, y=a; 
  while (b) {
    if (b%2) {
      x *= y;
      if (x>mod) x %= mod;
    }
    y *= y;
    if (y > mod) y%=mod; 
    b /= 2;
  }
  return x;
}
$0]]></content>
  <tabTrigger>powmod</tabTrigger>
  <scope>source.c, source.objc, source.c++, source.objc++</scope>
</snippet>
