<snippet>
  <description>Mod Power</description>
  <content><![CDATA[int modpow(int a, ${1:long long} b) {
  if (!b) return 1;
  int mp = modpow(a, b/2);
  mp = ((long long)mp*mp) % MOD;
  if (b&1) mp = ((long long)mp*a) % MOD;
  return mp;
}
$0]]></content>
  <tabTrigger>modpow</tabTrigger>
  <scope>source.c, source.objc, source.c++, source.objc++</scope>
</snippet>
